#cloud-config
# OCI Always-Free ARM(Ampere A1) VM 부트스트랩 — Ubuntu 22.04/24.04(arm64) 기준.
# OCI 인스턴스 생성 시 "고급 옵션 → Cloud-init 스크립트"에 붙여넣는다.
# Docker + git 설치, 80/443 개방, 레포 클론까지. 비밀(.env)·compose up 은 SSH 접속 후 수동
# (시크릿을 cloud-init 에 두지 않기 위함). 자세한 절차는 deploy/oci/README.md 참고.

package_update: true
packages:
  - git
  - ca-certificates
  - curl
  - gnupg

write_files:
  # OCI Ubuntu 이미지는 기본 iptables 가 SSH 외 차단 — 80/443 허용 규칙 영구화.
  - path: /etc/protolive-firewall.sh
    permissions: '0755'
    content: |
      #!/usr/bin/env bash
      set -e
      iptables -I INPUT 5 -p tcp --dport 80 -j ACCEPT
      iptables -I INPUT 6 -p tcp --dport 443 -j ACCEPT
      netfilter-persistent save || (apt-get install -y iptables-persistent && netfilter-persistent save)

runcmd:
  # Docker 공식 설치(arm64)
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  - chmod a+r /etc/apt/keyrings/docker.asc
  - echo "deb [arch=arm64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" > /etc/apt/sources.list.d/docker.list
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  - systemctl enable --now docker
  # 방화벽(80/443)
  - bash /etc/protolive-firewall.sh
  # 레포 클론(공개 repo)
  - git clone https://github.com/blue45f/proto-live.git /opt/protolive

final_message: 'ProtoLive VM 준비 완료. SSH 접속 후 deploy/oci/README.md 의 절차(.env 작성 → docker compose up -d --build) 진행.'
