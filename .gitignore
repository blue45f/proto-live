node_modules/
dist/
dist-test/
.env
.env.*
!.env.example
.DS_Store
coverage/
*.tsbuildinfo
data/
backend/data/
# Frontend static demo accounts (non-secret mock data) must be tracked so the
# build is reproducible — the broad data/ rule above is for runtime stores only.
!frontend/src/data/
.idea/
