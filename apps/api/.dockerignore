# 빌드 컨텍스트(apps/api)에서 제외 — 이미지 내부에서 재설치/재빌드
node_modules
dist
dist-test
*.tsbuildinfo

# 런타임 JSON 스토어 데이터 (볼륨으로 영속화, 이미지에 굽지 않음)
data

# 로컬 환경/로그/OS 잡파일
.env
.env.*
!.env.example
*.log
.DS_Store

# Docker 자체
Dockerfile
.dockerignore
