{
  "name": "proto-live",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "./scripts/dev.sh",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test",
    "lint": "npm --prefix frontend run lint",
    "check": "npm run build && npm run lint && npm run test",
    "smoke": "python3 scripts/smoke_ui.py",
    "seed:test-accounts": "npm --prefix backend run seed:test-accounts",
    "seed:test-data": "npm --prefix backend run seed:test-data"
  }
}
