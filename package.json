{
  "name": "proto-live",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install --prefix apps/api && npm install --prefix apps/web",
    "dev": "./scripts/dev.sh",
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "test": "npm --prefix apps/api run test",
    "lint": "npm --prefix apps/web run lint",
    "check": "npm run build && npm run lint && npm run test",
    "smoke": "python3 scripts/smoke_ui.py",
    "seed:demo-data": "npm --prefix apps/api run seed:test-data -- --reset",
    "seed:test-accounts": "npm --prefix apps/api run seed:test-accounts",
    "seed:test-data": "npm --prefix apps/api run seed:test-data"
  }
}
